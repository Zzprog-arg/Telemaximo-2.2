<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Registry for empty or library attribution tags to satisfy AppOps audits on Android 11/12+ -->
    <attribution
        android:tag="@string/empty_string"
        android:label="@string/app_name" />
    <attribution
        android:tag="null"
        android:label="@string/app_name" />
    <attribution
        android:tag="PlayBillingLibrary"
        android:label="@string/app_name" />
    <attribution
        android:tag="attributionTag"
        android:label="@string/app_name" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/img_telemaximo_banner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false"
            android:label="IPTV Admin Panel"
            android:theme="@style/Theme.MyApplication">
        </activity>
    </application>

</manifest>
